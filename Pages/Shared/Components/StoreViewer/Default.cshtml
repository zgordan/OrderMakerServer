@model Mtd.OrderMaker.Server.Components.Store.StoreViewerModel

<div id="@Model.ViewerId-viewer" style="display: flex; align-items:center">
    <div>
        <img id="@Model.ViewerId-viewer-img" src="@Model.ImgSrc" style="max-height: 64px; max-width: 120px;" />
    </div>
    <div id="@Model.ViewerId-viewer-form-name">
        @Model.FormName
    </div>
    <div id="@Model.ViewerId-viewer-doc-name">
        @Model.DocName
    </div>
</div>